name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20, 22]
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # eslint-plugin-unicorn needs iterator helpers (Node >=22); the published
      # package itself still supports Node 20, which the rest of this job covers.
      - run: pnpm lint
        if: matrix.node-version == 22

      - run: pnpm typecheck

      - run: pnpm test

      - run: pnpm build
